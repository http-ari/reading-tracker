# Reading Tracker

A local-first reading tracker for 50-minute Pomodoro sessions, built with React, TypeScript, Vite, and Recharts.

## Local development

```bash
pnpm install
pnpm dev
```

## Deployment

Pushes to `main` deploy automatically through GitHub Pages. In the repository settings, set **Pages → Build and deployment → Source** to **GitHub Actions**.

